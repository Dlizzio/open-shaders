name: "CI: Prebuilt Shader Cache"

on:
    push:
        branches: [ci_test/cache-validation-se-vr-20260920]
    workflow_dispatch:
        inputs:
            ref:
                description: "Same-repository ref to validate without publishing"
                required: true
                default: dev
                type: string

permissions:
    contents: read

jobs:
    cache:
        if: github.repository == 'Dlizzio/open-shaders'
        cache-mode: read
        uses: ./.github/workflows/_shared-build.yaml
        with:
            ref: ${{ inputs.ref || github.sha }}
            repository: ${{ github.repository }}
            run-cpp: false
            run-shader-validation: true
            run-shader-tests: false
            run-cpp-tests: false
            run-full-config-validation: false
            cache-artifacts: true
